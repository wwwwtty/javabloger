<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>

	<!-- include节点是struts2中组件化的方式 可以将每个功能模块独立到一个xml配置文件中 然后用include节点引用 -->
	<include file="struts-default.xml"></include>
	<!-- 自定义支持Rest风格类 -->
	<bean  type="org.apache.struts2.dispatcher.mapper.ActionMapper" name="rest" class="com.exam.utils.RestfulActionMapper"></bean>

    <!-- Rest风格的路径配置 -->
    <constant name="struts.mapper.class" value="rest" />
    <!--  
    Because the REST plugin uses the Convention plugin, some settings need to be set in struts.xml:
	-->
	<constant name="struts.convention.action.suffix" value="Controller"/>
	<constant name="struts.convention.action.mapAllMatches" value="true"/>
	<constant name="struts.convention.default.parent.package" value="rest-default"/>
	

	<!-- 管理页面的基础包 -->
	<package name="defaultAction" extends="struts-default" >
	   	<!--  
		<global-results>			
			< - -  全局异常  - -  >
			<result name="error" type="dispatcher">/error.jsp</result>
		</global-results>
		
		<global-exception-mappings>		
			<exception-mapping result="error" exception="java.lang.Exception"></exception-mapping>
		</global-exception-mappings>
		-->
		
		<action name="struts/student/*/*" class="{1}Action" method="{2}">
			<result name="success">/student/${target}.jsp</result>
		</action>


	</package>
	
</struts>